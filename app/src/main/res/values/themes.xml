<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ContactsApp" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ContactsApp" parent="Base.Theme.ContactsApp" />
    <style name="text_edit_style" parent="Theme.AppCompat.DayNight">
        <item name="android:padding">15dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/edit_text_shape</item>
        <item name="backgroundTint">@color/btn_color</item>
        <item name="android:layout_margin">8dp</item>
    </style>
    <style name="btn_edit_style" parent="Theme.AppCompat.DayNight">
        <item name="android:padding">15dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/edit_text_shape</item>
        <item name="backgroundTint">#D87EBEF1</item>
        <item name="android:layout_margin">8dp</item>
    </style>
</resources>